# Dev/test-only dependencies — not needed to run the app, only to test and
# lint it. Installed on top of requirements.txt.
-r requirements.txt

ruff>=0.6.0
pytest>=8.0.0
pytest-asyncio>=0.24.0
httpx>=0.27.0 # FastAPI's TestClient needs it

# Terraform static analysis (Phase 9) — misconfiguration scanning without
# ever touching real AWS.
checkov>=3.2.0
